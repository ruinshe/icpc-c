#include <iostream>
#include <functional>
#include <numeric>
#include <cstring>
#include <algorithm>

std::istream& in = std::cin;
std::ostream& out = std::cout;

const int MaxN = 5005;

void solve() {
}

int main() {
  std::ios_base::sync_with_stdio(false);
  out.precision(16);
  solve();
  return 0;
}
